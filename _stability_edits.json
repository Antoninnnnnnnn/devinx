[
{"path":"README.md","before":"536ec24c272cdaba9797836b60f9874e0f17c90a7a783bd3e9fc7478a2f0226c","after":"5ab9bc2a72e380dbb5ec75c39cad31dc00fc25346b87686a2a67633d14c2cd33","edits":[[399,399,"## Local diagnostics and stability controls\n\n`devinx --status`, `devinx --doctor --json`, and `devinx --explain --cx --or`\ninspect the running service without starting it or spending inference quota.\nThe dashboard also shows current requests and their phase. See\n[stability controls and validation](docs/stability.md) for the new resource\nbounds, opt-in strict compaction, shutdown behaviour, test coverage and limits.\n\n"]]},
{"path":"devinx.py","before":"f1fe04b84f04360eb2339df021e4505578992e74ca54f60f983d9e9068c8c20f","after":"95c924f314f0e4f7458da02454df0946859628a9f8c029ca1f397c61a8a525d9","edits":[
[29,29,"import io\n"],
[42,42,"from runtime_support import CONFIG_FIELDS, build_id\n"],
[288,289,"    return build_id(HERE)\n"],
[290,301,"\ndef effective_configuration():\n    return {env: globals().get(name) for env, name in CONFIG_FIELDS.items()}\n"],
[309,309,"_active_requests = {}\n_request_local = threading.local()\nMAX_INFLIGHT = int(os.environ.get(\"DEVINX_MAX_INFLIGHT\", \"64\"))\nHTTP_READ_TIMEOUT = float(os.environ.get(\"DEVINX_HTTP_READ_TIMEOUT\", \"30\"))\nCLIENT_WRITE_TIMEOUT = float(os.environ.get(\"DEVINX_CLIENT_WRITE_TIMEOUT\", \"120\"))\nRELAY_READ_TIMEOUT = float(os.environ.get(\"DEVINX_RELAY_READ_TIMEOUT\", \"0\")) or None\n\n\ndef _request_phase(phase, model=None):\n    key = getattr(_request_local, \"key\", None)\n    with _inflight_lock:\n        entry = _active_requests.get(key)\n        if entry is None:\n            return\n        if phase != entry[\"phase\"]:\n            entry[\"phase\"] = phase\n            entry[\"phase_at\"] = time.monotonic()\n        if model is not None:\n            entry[\"model\"] = model\n\n\ndef live_requests():\n    now = time.monotonic()\n    with _inflight_lock:\n        return [{\"id\": e[\"id\"], \"model\": e[\"model\"], \"phase\": e[\"phase\"],\n                 \"elapsed\": round(now - e[\"started\"], 1),\n                 \"phase_elapsed\": round(now - e[\"phase_at\"], 1)}\n                for e in _active_requests.values()]\n"],
[312,312,"    key, now = uuid.uuid4().hex[:12], time.monotonic()\n"],
[313,313,"        if MAX_INFLIGHT > 0 and _inflight[\"n\"] >= MAX_INFLIGHT:\n            return False\n"],
[314,314,"        _active_requests[key] = {\"id\": key, \"model\": None,\n                                 \"phase\": \"reading_body\", \"started\": now,\n                                 \"phase_at\": now}\n    _request_local.key = key\n    return True\n"],
[317,317,"    key = getattr(_request_local, \"key\", None)\n"],
[318,319,"        if _active_requests.pop(key, None) is not None:\n            _inflight[\"n\"] -= 1\n    _request_local.key = None\n"],
[1035,1035,"MAX_FRAME_BYTES = int(os.environ.get(\"DEVINX_MAX_FRAME\", str(16 * 1024 * 1024)))\nMAX_INFLATED_FRAME_BYTES = int(os.environ.get(\n    \"DEVINX_MAX_INFLATED_FRAME\", str(64 * 1024 * 1024)))\n\n\ndef _frame_payload(payload, compressed):\n    if not compressed:\n        if len(payload) > MAX_INFLATED_FRAME_BYTES:\n            raise ValueError(\"upstream frame exceeds the decoded size limit\")\n        return payload\n    with gzip.GzipFile(fileobj=io.BytesIO(payload)) as fh:\n        raw = fh.read(MAX_INFLATED_FRAME_BYTES + 1)\n    if len(raw) > MAX_INFLATED_FRAME_BYTES:\n        raise ValueError(\"upstream frame exceeds the decoded size limit\")\n    return raw\n\n\n"],
[1047,1047,"    _request_phase(\"summarizing\" if purpose == \"summary\" else \"authenticating\")\n"],
[1061,1061,"            _request_phase(\"summarizing\" if purpose == \"summary\" else \"connecting\")\n"],
[1094,1094,"        _request_phase(\"summarizing\" if purpose == \"summary\" else \"waiting_first_frame\")\n"],
[1102,1102,"                if ln > MAX_FRAME_BYTES:\n                    raise ValueError(\"upstream frame exceeds the wire size limit\")\n"],
[1108,1109,"                    trailer = _frame_payload(payload, flag & 1)\n"],
[1123,1124,"                raw = _frame_payload(payload, flag & 1)\n"],
[1128,1128,"                    _request_phase(\"summarizing\" if purpose == \"summary\" else \"streaming\")\n"],
[1201,1201,"_summary_flights = {}\nCOMPACT_STRICT = os.environ.get(\"DEVINX_COMPACT_STRICT\") == \"1\"\n\n\nclass CompactionUnavailable(RuntimeError):\n    \"\"\"Strict compaction could not preserve the input safely.\"\"\"\n\n\n@contextlib.contextmanager\ndef _summary_guard(key):\n    with _summary_lock:\n        flight = _summary_flights.setdefault(key, [threading.Lock(), 0])\n        flight[1] += 1\n    try:\n        with flight[0]:\n            yield\n    finally:\n        with _summary_lock:\n            flight[1] -= 1\n            if not flight[1]:\n                _summary_flights.pop(key, None)\n\n\ndef _block_hashes(span):\n    return tuple(hashlib.sha256(json.dumps(pair, sort_keys=True,\n                 ensure_ascii=False, default=str).encode()).hexdigest()\n                 for pair in span)\n\n\ndef _uncovered_blocks(span, previous, flattened=False):\n    \"\"\"Return new blocks, or None if previously summarised content changed.\n\n    Ordinary histories must extend a prefix. Legacy flattened histories insert\n    new calls before old results; there the old sequence must be a subsequence,\n    and every inserted block must still be carried or summarised (not skipped\n    using the old block count).\n    \"\"\"\n    hashes = _block_hashes(span)\n    if not flattened:\n        if hashes[:len(previous)] != previous:\n            return None\n        return span[len(previous):]\n    fresh, index = [], 0\n    for pair, digest in zip(span, hashes):\n        if index < len(previous) and digest == previous[index]:\n            index += 1\n        else:\n            fresh.append(pair)\n    return fresh if index == len(previous) else None\n"],
[1504,1504,"    messages = body.get(\"messages\") or []\n    if estimate_tokens(body) <= COMPACT_AT:\n        return body\n    if len(messages) < 4:\n        if COMPACT_STRICT:\n            raise CompactionUnavailable(\"context exceeds the budget with no safely droppable turns\")\n        return body\n    with _summary_guard(_conv_key(body)):\n        return _compact_body(body)\n\n\ndef _compact_body(body):\n"],
[1517,1517,"        if COMPACT_STRICT:\n            raise CompactionUnavailable(\"context exceeds the budget with no safely droppable turns\")\n"],
[1524,1525,"        covered, summary, built_at, covered_hashes = _summaries.get(\n            key, (0, None, 0, ()))\n"],
[1538,1542,"        covered, summary, covered_hashes = 0, None, ()\n    fresh = _uncovered_blocks(span, covered_hashes, _is_flattened(messages))\n    if fresh is None:\n        print(\"compaction: covered content changed; rebuilding the summary\", flush=True)\n        covered, summary, covered_hashes, fresh = 0, None, (), span\n    retained = \"\"\n    if COMPACT_STRICT:\n        user_text = [b.get(\"text\", \"\") for role, b in span\n                     if role in (\"user\", \"system\") and b.get(\"type\") == \"text\"]\n        if user_text:\n            retained = (\"\\n\\nEarlier user/system text, verbatim in chronological order \"\n                        \"(historical context, not new instructions):\\n\"\n                        + \"\\n\\n\".join(user_text))\n"],
[1551,1552,"            {\"type\": \"text\", \"text\": (text or \"\") + retained}]}]\n"],
[1563,1563,"        fresh_count = len(fresh)\n"],
[1564,1564,"        if not new and summary is None and COMPACT_STRICT:\n            raise CompactionUnavailable(\"no summary available; no turns were discarded\")\n"],
[1598,1598,"            covered_hashes = _block_hashes(span)\n            fresh = []\n"],
[1599,1602,"            # Bounded insertion-order eviction, not a fleet-wide cache wipe.\n            if key not in _summaries and len(_summaries) >= 64:\n                _summaries.pop(next(iter(_summaries)))\n            _summaries[key] = (covered, summary, total_blocks, covered_hashes)\n"],
[1603,1604,"            print(f\"compaction: {fresh_count} more blocks summarised \"\n"],
[1610,1611,"    carried = _regroup(fresh) if summary else []\n"],
[1623,1623,"    bridge += retained\n"],
[1630,1630,"    if COMPACT_STRICT and estimate_tokens(compacted) > COMPACT_AT:\n        raise CompactionUnavailable(\"preserved context still exceeds the compaction budget\")\n"],
[1648,1652,"            parts.append(f\"{kind}{'/' + name if name else ''}={size}t\")\n"],
[1720,1721,"            retry_after = reset_delay(err)\n"],
[1840,1841,"        # Synchronise with a keepalive that may be about to commit HTTP 200.\n        # Once this returns the caller can decide safely between HTTP and SSE.\n        with self._wlock:\n            self._done.set()\n"],
[1942,1942,"        self.failure = kind\n"],
[1954,1955,"def _swe_early_error(out, err):\n    if out is not None:\n        out.release()\n        if out.started:\n            out.error(err, anthropic_error(err)[0])\n            out.stop()\n            return None, None\n    return None, err\n\n\ndef run_swe(body, wfile, make_stream=None, outcome=None):\n    \"\"\"Run a turn, keeping the streaming lifecycle and error contract shared.\n\n    `outcome` is an optional local diagnostic dict, never response content.\n    A 200 stream that later fails must not be logged as a successful turn.\n    \"\"\"\n    emitter = make_stream or AnthropicStream\n    out = emitter(wfile, resolve_model(body)) if body.get(\"stream\") else None\n    try:\n        if out is not None:\n            out.arm()\n        response, err = _run_swe(body, out)\n        if outcome is not None:\n            outcome[\"status\"] = (str(anthropic_error(err)[1]) if err else\n                                 \"stream_error\" if getattr(out, \"failure\", None)\n                                 else \"200\")\n        return response, err\n    finally:\n        if out is not None:\n            out.release()\n\n\ndef _run_swe(body, out):\n"],
[1962,1964,""],
[1970,1970,"        _request_phase(\"compacting\")\n"],
[1972,1972,"    except CompactionUnavailable as e:\n        return _swe_early_error(out, f\"unavailable: strict compaction: {e}\")\n"],
[1973,1973,"        if COMPACT_STRICT:\n            # Strict mode must not silently become lossy on an unexpected\n            # summariser/parser failure. Do not include prompt-bearing details.\n            return _swe_early_error(out, \"unavailable: strict compaction failed\")\n"],
[1978,1982,""],
[1987,1989,"        once a message_start is on the wire an HTTP status is no longer\n        expressible. A retry before content can reuse that same emitter.\n"],
[1999,1999,"            _request_phase(\"preparing\")\n"],
[2007,2010,"            return _swe_early_error(out, f\"request build: {e}\")\n"],
[2021,2021,"          _request_phase(\"waiting_capacity\")\n"],
[2089,2090,"        if (err and not emitted and retries < NETWORK_RETRIES\n"],
[2097,2098,"        if err and not emitted and \"resource_exhausted\" in err:\n"],
[2117,2117,"                _request_phase(\"waiting_rate_limit\")\n"],
[2124,2125,"        if err and not emitted and attempt < 2 and \"permission_denied\" in err:\n"],
[2150,2150,"        if err:\n            # Freeze the heartbeat before deciding which response is legal.\n            out.release()\n"],
[2536,2539,"        if stop_reason == \"max_tokens\":\n            response = self._response(\"incomplete\", usage)\n            response[\"incomplete_details\"] = {\"reason\": \"max_output_tokens\"}\n            self._send(\"response.incomplete\", {\n                \"type\": \"response.incomplete\", \"response\": response})\n        else:\n            self._send(\"response.completed\", {\n                \"type\": \"response.completed\",\n                \"response\": self._response(\"completed\", usage)})\n"],
[2540,2542,"    def error(self, message, kind=\"api_error\"):\n        self.failure = kind\n        self._send(\"error\", {\"type\": \"error\", \"code\": kind,\n"],
[2568,2572,"# while `connect` only covers DNS/TCP/TLS establishment. Restrict read retries\n# to safe methods: a POST can have been processed even when its response headers\n# never reached us. Application-level SWE recovery remains explicit and bounded;\n# this adapter must not silently multiply it, or replay a relayed generation.\n"],
[2573,2574,"               allowed_methods=frozenset({\"GET\", \"HEAD\", \"OPTIONS\"}),\n               backoff_factor=0.2)\n"],
[2581,2581,"    timeout = HTTP_READ_TIMEOUT  # header/body inactivity, not model latency\n"],
[2764,2765,"                    capture_output=True, text=True, timeout=60,\n                    env=dict(os.environ, DEVINX_LOG=os.environ.get(\n                        \"DEVINX_LOG\", os.path.join(DATA_DIR, \"devinx.log\"))))\n"],
[2799,2799,"        payload[\"active_requests\"] = live_requests()\n"],
[2914,2915,"                                 \"inflight\": busy,\n                                 \"configuration\": effective_configuration()})\n"],
[2941,2942,"        if not _enter_request():\n            self.send_error_json(503, \"overloaded_error\",\n                                 \"Local request capacity reached; retry later\", 1)\n            return\n"],
[2954,2955,"            lengths = self.headers.get_all(\"content-length\", [])\n            if (self.headers.get(\"transfer-encoding\") is not None\n                    or len(lengths) != 1\n                    or re.fullmatch(r\"[0-9]+\", lengths[0].strip()) is None):\n                self.send_error_json(400, \"invalid_request_error\",\n                                     \"One non-negative Content-Length is required\")\n                return\n            length = int(lengths[0])\n"],
[2960,2960,"            if len(raw) != length:\n                self.send_error_json(400, \"invalid_request_error\", \"Incomplete body\")\n                return\n"],
[2961,2961,"            self.connection.settimeout(CLIENT_WRITE_TIMEOUT)\n"],
[2987,2987,"        _request_phase(\"accepted\", model=model)\n"],
[3045,3046,"                result = {}\n                _, err = run_swe(body, self.wfile, outcome=result)\n                outcome = result.get(\"status\", \"200\")\n"],
[3073,3074,"        \"\"\"SWE-2 over the Responses wire, with the same errors as Messages.\"\"\"\n"],
[3081,3083,""],
[3086,3086,"        started, result, outcome = time.monotonic(), {}, \"200\"\n"],
[3087,3089,"            _, err = run_swe(translated, self.wfile,\n                            lambda w, m: ResponsesStream(w, m, custom),\n                            outcome=result)\n            outcome = result.get(\"status\", \"200\")\n            if err:\n                kind, status, message, wait = anthropic_error(err, translated)\n                outcome = str(status)\n                self.send_error_json(status, kind, message, wait)\n"],
[3093,3093,"            outcome = type(e).__name__\n"],
[3094,3095,"                  f\"status={outcome}\", flush=True)\n"],
[3096,3096,"            print(f\"route=swe-responses model={resolve_model(translated)} \"\n                  f\"status={outcome} in {time.monotonic() - started:.1f}s\",\n                  flush=True)\n"],
[3107,3107,"            _request_phase(\"connecting\")\n"],
[3109,3110,"                                       allow_redirects=False,\n                                       timeout=(15, RELAY_READ_TIMEOUT))\n            _request_phase(\"relaying\")\n"],
[3274,3276,"def drain_and_exit(srv, extra_servers=()):\n    \"\"\"Close listeners, then drain accepted turns without blocking signals.\n"],
[3277,3281,"    The signal handler runs on the serve_forever thread. It must return so that\n    shutdown(), running on another thread, can finish. The coordinator is NOT\n    a daemon: once serve_forever returns it keeps Python alive for accepted work.\n    Repeated SIGTERM/SIGINT requests do not start competing shutdown workers.\n"],
[3282,3294,"    requested = False\n    servers = (srv,) + tuple(s for s in extra_servers if s is not None)\n"],
[3295,3298,"    def drain():\n        deadline = time.monotonic() + DRAIN_SECONDS\n        for server in servers:\n            try:\n                server.shutdown()\n            finally:\n                server.server_close()\n        while time.monotonic() < deadline:\n"],
[3302,3303,"            time.sleep(0.05)\n"],
[3305,3306,"        print(\"devinx: draining complete, exiting\"\n"],
[3309,3309,"\n    def handler(signum, frame):\n        nonlocal requested\n        if requested:\n            return\n        requested = True\n        threading.Thread(target=drain, name=\"devinx-drain\", daemon=False).start()\n"],
[3362,3362,"    return srv\n"],
[3369,3371,""],
[3372,3373,"    _dashboard_srv = serve_dashboard_port(DASHBOARD_PORT) if DASHBOARD_PORT else None\n    drain_and_exit(_srv, (_dashboard_srv,))\n"]
]},
{"path":"launcher.py","before":"3047bd6388aac2c59af14328cf8a0a12e515098f5ee60d1751acc25ba0f224cc","after":"61925e89c523e2ab84ecf811aa9b8b6fb2adf31d41de8fa795f798ece3043c4a","edits":[
[22,22,"from runtime_support import build_id, startup_lock\n"],
[344,350,"    value = build_id(HERE)\n    return None if value == \"unknown\" else value\n"],
[461,461,"def ensure_service():\n    \"\"\"Perform the readiness/start sequence once across concurrent launchers.\"\"\"\n    try:\n        with startup_lock(data_dir(), PORT, START_TIMEOUT):\n            state, info = service_state()\n            if state == \"foreign\":\n                sys.stderr.write(f\"devinx: another service owns {HOST}:{PORT}; not starting or stopping it\\n\")\n                return False\n            if state == \"stale\":\n                # Restarting under a turn that is mid-flight would cut it in half, so a\n                # busy service is reported rather than replaced.\n                if info.get(\"inflight\"):\n                    sys.stderr.write(\n                        f\"devinx: the service on {PORT} is running older code and is \"\n                        f\"busy ({info['inflight']} request(s) in flight).\\n\"\n                        f\"        Continuing with it. Restart when it is idle: \"\n                        f\"kill {info.get('pid', '<pid>')}\\n\")\n                    state = \"fresh\"\n                elif stop_service(info):\n                    print(\"devinx: replacing a service running older code\", flush=True)\n                    state = \"absent\"\n                else:\n                    sys.stderr.write(\n                        \"devinx: the service on this port is running older code and \"\n                        \"could not be stopped automatically.\\n\"\n                        \"        Stop it by hand and run again\"\n                        + (f\": kill {info['pid']}\\n\" if info.get(\"pid\") else \".\\n\"))\n                    state = \"fresh\"\n\n            if state != \"fresh\":\n                start_service()\n                deadline = time.time() + START_TIMEOUT\n                while time.time() < deadline:\n                    if listening():\n                        break\n                    time.sleep(0.5)\n                else:\n                    sys.stderr.write(\n                        f\"devinx failed to start on {HOST}:{PORT}\\n\"\n                        f\"check {os.path.join(data_dir(), 'devinx.log')}\\n\")\n                    return False\n            return True\n    except (OSError, TimeoutError) as error:\n        sys.stderr.write(f\"devinx: could not coordinate service startup: {error}\\n\")\n        return False\n\n\n"],
[462,462,"    if len(sys.argv) > 1 and sys.argv[1] in (\"--doctor\", \"--status\", \"--explain\"):\n        import diagnostics\n        return diagnostics.main(sys.argv[1], sys.argv[2:], sys.modules[__name__])\n"],
[486,520,"    if not ensure_service():\n        return 1\n"]
]},
{"path":"tests/test_regressions.py","before":"cdf0df966778025306ed37643cdb46c43a06e60601728685146284064d857e83","after":"17a8407976a2461b888bc2eb2275c604ee7e3c52f539f81683af8274b0621dc1","edits":[
[21,22,"try:\n    import tomllib\nexcept ImportError:  # Python 3.10, tests only\n    import tomli as tomllib\n"],
[332,332,"        # Real uncovered blocks are now tracked by content; the old counter\n        # accidentally carried old results twice. Use a budget that actually\n        # fills with the new blocks in this fixture, keeping the same assertions.\n        budget = mock.patch.object(devinx, \"COMPACT_AT\", 100000)\n        budget.start()\n        self.addCleanup(budget.stop)\n"],
[516,517,"        fake_accounts = [\n            {\"name\": \"test-a\", \"blocked_until\": 0},\n            {\"name\": \"test-b\", \"blocked_until\": 0},\n        ]\n        with mock.patch.object(devinx, \"_accounts\", fake_accounts), \\\n             mock.patch.object(devinx, \"_turn\", {\"n\": 0}), \\\n             mock.patch.object(devinx, \"chat_stream\", flaky), \\\n"],
[600,601,"        with mock.patch.object(devinx, \"api_key\", return_value=\"test-only\"):\n            req, _ = devinx.build_request(body)\n"]
]},
{"path":"tools/dashboard.html","before":"71fe0afd5de220fdbbd7f3afaa6aedf6358ec05fad15aaf6db4e0f7812eb9bce","after":"1b9cc3d07156a9f692c8a07cbffa8583f7caa723dd9b3a59bc2740eff11f1bf5","edits":[
[350,350,"<h2>Requêtes actives maintenant</h2>\n<div class=\"card\">\n  <div class=\"tw\"><table><thead><tr><th>Requête</th><th>Modèle</th><th>État</th>\n    <th class=\"n\">Durée totale</th><th class=\"n\">Dans cet état</th></tr></thead>\n    <tbody id=\"active-requests\"></tbody></table></div>\n  <div class=\"note\" id=\"active-note\">État actuel, indépendant de la période sélectionnée. Les identifiants sont ceux des requêtes, pas des agents.</div>\n</div>\n"],
[430,430,"function renderActiveRequests(items){\n  const target = $(\"active-requests\");\n  target.replaceChildren();\n  const labels = {reading_body:\"Lecture de la requête\", accepted:\"Acceptée\",\n    compacting:\"Préparation du contexte\", summarizing:\"Résumé du contexte\",\n    preparing:\"Préparation\", waiting_capacity:\"Attente de capacité\",\n    authenticating:\"Authentification\", connecting:\"Connexion au fournisseur\",\n    waiting_first_frame:\"Attente du fournisseur\", streaming:\"Réception\",\n    waiting_rate_limit:\"Attente après limitation\", relaying:\"Relais en cours\"};\n  if (!Array.isArray(items) || items.length === 0){\n    const tr = document.createElement(\"tr\"), td = document.createElement(\"td\");\n    td.colSpan = 5;\n    td.textContent = Array.isArray(items) ? \"Aucune requête active.\" : \"Non disponible sur cette version du service.\";\n    tr.appendChild(td); target.appendChild(tr); return;\n  }\n  for (const item of items){\n    if (!item || typeof item !== \"object\") continue;\n    const tr = document.createElement(\"tr\");\n    const values = [item.id || \"—\", item.model || \"—\", labels[item.phase] || \"État inconnu\",\n                    dur(item.elapsed), dur(item.phase_elapsed)];\n    values.forEach((value, index) => {\n      const td = document.createElement(\"td\");\n      td.textContent = String(value);\n      if (index >= 3) td.className = \"n\";\n      tr.appendChild(td);\n    });\n    target.appendChild(tr);\n  }\n}\n"],
[431,431,"  renderActiveRequests(d.active_requests);\n"]
]}
]
