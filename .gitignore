.venv/
__pycache__/
*.pyc

# Runtime state and diagnostics, never part of the package
devinx.log
*.log
install-smoke.log

# TLS material for the tailnet mount: a private key never belongs in the repo
*.crt
*.key

# Worktrees created by agents; never part of the repo
.claude/worktrees/
